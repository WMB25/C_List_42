#include <unistd.h>

void ft_print_combn(int n);

int main(void)
{
	ft_print_combn(2);
	return (0);
}


void ft_print_combn(int n)
{

	char num_char_one, num_char_two;

	if(n < 0 && n > 10)
	{
		for(int i = 0; i < 10; i++)
		{
			num_char_one = i + '0';
			num_char_two = j + '0';
			write(1, &num_char_one, 1);
			write(1, &num_char_two, 1);
		}
	}
}
